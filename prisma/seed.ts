import "dotenv/config";

export async function main() {
  // No seed data — production uses migrate deploy only.
  // The previous seed targeted a tutorial Post model that is not in the current schema.
}

void main();
